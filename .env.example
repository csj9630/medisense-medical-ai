APP_NAME=Medical AI API
APP_ENV=local
API_PREFIX=/api
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require&channel_binding=require
MODEL_SCHEMAS=app_db,vector_db
VITE_API_URL=http://localhost:8000/api
JWT_SECRET_KEY=change-this-to-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
PASSWORD_RESET_EXPIRE_MINUTES=30
# 배포 시 프론트 도메인을 지정합니다. 아래 콜백 주소를 비우면 이 값에서 자동 생성됩니다.
FRONTEND_URL=http://localhost:5173
GOOGLE_OAUTH_REDIRECT_URI=
GITHUB_OAUTH_REDIRECT_URI=
EMAIL_VERIFICATION_EXPIRE_MINUTES=10
MESSAGE_MAX_FILES_PER_REQUEST=5
MESSAGE_MAX_FILE_SIZE_MB=20

# SMTP를 설정하지 않으면 로컬 환경에서 인증 코드가 API 응답에 포함됩니다.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_TIMEOUT_SECONDS=30

# Cloudflare R2 저장소 (프로필 이미지 및 Admin RAG 대용량 원본)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=https://assets.example.com
RAG_R2_BUCKET_NAME=

# 공통 ai/ocr Core (PaddleOCR 모델은 최초 OCR 실행 시 다운로드될 수 있습니다.)
OCR_MAX_FILE_SIZE_MB=8192
OCR_INLINE_FILE_SIZE_MB=20
OCR_LARGE_ARCHIVE_UNCOMPRESSED_SIZE_MB=16384
OCR_MAX_PDF_PAGES=50
OCR_NATIVE_TEXT_MIN_CHARS=20
OCR_SIGNIFICANT_IMAGE_AREA_RATIO=0.03
OCR_PDF_RENDER_DPI=200
OCR_MAX_IMAGE_SIDE=2400
OCR_MAX_IMAGE_PIXELS=40000000
OCR_PADDLE_DEVICE=cpu
OCR_PADDLE_LANGUAGE=korean
OCR_MIN_CONFIDENCE=0.5
OCR_ENABLE_DENOISE=true
OCR_ENABLE_DESKEW=true
OCR_JOB_TTL_MINUTES=60
OCR_MAX_PENDING_JOBS=5
OCR_MAX_OFFICE_UNCOMPRESSED_SIZE_MB=200
OCR_MAX_OFFICE_ARCHIVE_ENTRIES=5000

# Admin OCR 웹페이지 URL 수집 한도 (SSRF 방어 및 Gemini/Neon 저장량 보호)
OCR_WEB_MAX_URL_LENGTH=500
OCR_WEB_MAX_HTML_SIZE_MB=5
OCR_WEB_MAX_TEXT_CHARS=100000
OCR_WEB_MAX_CHUNKS=200
OCR_WEB_MAX_REDIRECTS=3
OCR_WEB_CONNECT_TIMEOUT_SECONDS=5
OCR_WEB_READ_TIMEOUT_SECONDS=15
OCR_WEB_MAX_IMAGES=20
OCR_WEB_MAX_IMAGE_SIZE_MB=5
OCR_WEB_MAX_TOTAL_IMAGE_SIZE_MB=30
OCR_WEB_IMAGE_CONCURRENCY=4

# Vast.ai 2× V100에서 실행하는 5개 모델 공통 원격 추론 API
LLM_REMOTE_ENABLED=false
LLM_REMOTE_BASE_URL=http://your-vast-host:published-port
LLM_REMOTE_API_KEY=
LLM_REMOTE_GEMMA_MODEL=gemma
LLM_REMOTE_MEDGEMMA_FINAL_MODEL=medgemma-final
LLM_REMOTE_MEDGEMMA_DATASET_MODEL=medgemma-dataset
LLM_REMOTE_QWEN_MODEL=qwen
LLM_REMOTE_LLAMA_MODEL=llama
LLM_REMOTE_TIMEOUT_SECONDS=300
LLM_REMOTE_MAX_CONCURRENCY=5

# Vast.ai Jina v4 + Medical BGE-M3 이중 Embedding API
# Base URL에는 /v1/embeddings를 붙이지 않습니다.
EMBEDDING_REMOTE_BASE_URL=https://embedding.example.com
EMBEDDING_API_KEY=replace-with-the-embedding-api-key
EMBEDDING_JINA_MODEL=jina-v4
EMBEDDING_BGE_MODEL=medical-bgem3
EMBEDDING_DIMENSION=1024
EMBEDDING_TIMEOUT_SECONDS=60
EMBEDDING_BATCH_SIZE=32

# OCR 저장과 메인 채팅 RAG가 위 두 Provider를 공통으로 사용합니다.
RAG_EMBEDDING_PROVIDER=remote_dual
# 로컬 단일 모델로 개발할 때만 sentence_transformer와 아래 옵션을 사용합니다.
RAG_EMBEDDING_MODEL_NAME=
RAG_EMBEDDING_DIMENSION=
RAG_EMBEDDING_TRUNCATE_DIM=
RAG_EMBEDDING_REVISION=
RAG_EMBEDDING_TRUST_REMOTE_CODE=false
RAG_EMBEDDING_QUERY_PROMPT_NAME=
RAG_EMBEDDING_DOCUMENT_PROMPT_NAME=

# 질병관리청(KDCA) 공공 건강정보 OpenAPI - scripts/rag_ingest.py --source kdca-openapi 실행 전 필요
KDCA_HEALTHINFO_TOKEN=
